.ui.grid
  .two.wide.mobile.column
    %a{href: "#{requeriments_path}"}
      %i.big.arrow.left.icon
  .thirteen.wide.mobile.column.text-center
    .title-header Criação de requerimento
    .sub-title Formulário de criação de renda

.big-divider
.ui.grid.container
  .column.content-form
    - if current_cadastre.assessments.where(finalized: false).present? 
      %span{style: 'text-align: center'}
        %i Para abertura de novo requerimento aguarde a resposta dos protocolos já iniciados.
    - else
      - if [3,4].include?(current_cadastre.current_situation_id) 
        %span{style: 'text-align: center'}
          %i A abertura de novo requerimento só poderá ser realizada por candidatos que estiverem na situação de "HABILITADO" e "CONVOCADO PARA ENTREGA DE DOCUMENTAÇÃO".
      - else
        = render 'form'
