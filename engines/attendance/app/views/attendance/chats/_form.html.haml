= simple_form_for @chat, url: chats_path, method: :post do |f|
  = f.label "Assunto"
  = f.association :chat_category, label: false, prompt: 'Selecione'
  .big-divider
  .column.text-center
    = f.submit "Enviar", class: 'ui button yellow'
    = link_to "Cancelar", chats_path, class: 'ui button gray'
