= simple_form_for @chat, url: chats_path, method: :post do |f|
  = f.association :chat_category, label: "Assunto", prompt: 'Selecione'
  = f.input :content
  .mini-margin
  = f.submit "Iniciar conversa", class: 'ui button green fluid', data: { disable_with: t(:disable_with) }
